apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: helep
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
    - port: {{ .Values.servicePort }}
      targetPort: {{ .Values.servicePort }}
  type: ClusterIP
